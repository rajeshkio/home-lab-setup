apiVersion: gateway.networking.k8s.io/v1 
kind: Gateway
metadata:
  name: k3s-server-gateway
spec:
  gatewayClassName: istio
  listeners:
  - protocol: HTTPS
    port: 443
    name: https-listener
    allowedRoutes:
      namespaces:
        from: All 
